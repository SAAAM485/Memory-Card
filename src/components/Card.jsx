export default function Card({ character }) {
    return (
        <div className="card">
            <div className="card-content">
                <img src="" alt="" />
                <h1>{`${character}`}</h1>
            </div>
        </div>
    );
}
